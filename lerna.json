{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "version": {
      "message": "chore(Release): publish"
    },
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md", "*.test.ts"],
      "registry": "https://registry.npmjs.org"
    }
  }
}
